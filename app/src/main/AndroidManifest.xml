<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".cleanmvvm.HiltApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArchitecturePatterns"
        tools:targetApi="31">
        <activity
            android:name=".mvc.view_controller.CounterActivity"
            android:exported="true"
            android:theme="@style/Theme.ArchitecturePatterns.NoActionBar" />

        <activity
            android:name=".mvp.view.LuckyNumberActivity"
            android:exported="true"
            android:theme="@style/Theme.ArchitecturePatterns.NoActionBar" />

        <activity
            android:name=".mvvm.view.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ArchitecturePatterns.NoActionBar" />

        <activity
            android:name=".cleanmvvm.view.ui.NotesActivity"
            android:exported="true"
            android:theme="@style/Theme.ArchitecturePatterns.NoActionBar" />

        <activity
            android:name=".cleanmvvm.view.ui.RecipeActivity"
            android:exported="true"
            android:theme="@style/Theme.ArchitecturePatterns.NoActionBar" />
    </application>

</manifest>